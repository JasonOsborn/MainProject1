
// Movement override;; mailbox detected?
// CurrentlyIR[0]++ on IR overrride
// After deliver_mail, reset Move, reset detected
reg detected = 0;
if (detected && !(CurrentlyIR[1] == CurrentlyIR[0]))

	Move = 0;
	if ( mailbox && detected2)
		// deliver_mail
			CurrentlyIR[1] = CurrentlyIR[0]
		counter++;
	else if (mailbox 2 && detected2)
		// deliver_mail
			CurrentlyIR[1] = CurrentlyIR[0]
		counter++;
	else if (mailbox 3 && detected2)
		// deliver_mail
			CurrentlyIR[1] = CurrentlyIR[0]
		counter++;
	else if (detected2)
		reverse
		Move = 1;
else if (!detected)
	CurrentlyIR = 2'b01;


// Range of acceptable frequencies
integer n = -2
while (n < 3)
	if (freq  = desiredNum + n)
		mailbox = 1;
		n = -2
	n = n + 1;
