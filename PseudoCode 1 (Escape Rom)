
// Movement override;; mailbox detected?
// CurrentCount[0]++ on IR overrride
// After deliver_mail, reset Move, reset detected
reg detected = 0;
if (detected && !(CurrentCount[1] == CurrentCount[0]))

	Move = 0;
	if ( mailbox && detected2)
		// deliver_mail
			CurrentCount[1] = CurrentCount[0]
		counter++;
	else if (mailbox 2 && detected2)
		// deliver_mail
			CurrentCount[1] = CurrentCount[0]
		counter++;
	else if (mailbox 3 && detected2)
		// deliver_mail
			CurrentCount[1] = CurrentCount[0]
		counter++;
	else if (detected2)
		reverse
		Move = 1;
else if (!detected)
	CurrentCount = 2'b01;


// Range of acceptable frequencies
integer n = -2
while (n < 3)
	if (freq  = desiredNum + n)
		mailbox = 1;
		n = -2
	n = n + 1;
